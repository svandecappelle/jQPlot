// JavaScript Document function rotateElements(){alert('test');$('#rotating-elements').sideswap()}// JavaScript Document(function($){$.fn.sideswap = function(options){$.fn.sideswap.defaults ={navigation:true,previous:'prev',next:'next',display_time:5000,transition_speed:200,auto_run:true};,$.fn.sideswap.defaults,options),clicked = false;return this .each(function(){var $this = $(this),$parent = $this.parent(),timer = false,$images = $this.children(),count = $images.size();setup();function setup(){$this.css('position','relative');$images.css({"position":"absolute","top":"0px","left":"0px"}).removeClass('hide').hide();$this.children(':first').show();if(opts.navigation && count>1){createNavigation()}if(opts.auto_run && count>1){initiateInterval();$parent.hover(function(){clearInterval(timer)},function(){initiateInterval()})}}function rotate(direction){var $current_image = $this.children(':visible:first') == 'undefined' ? $this .children(':first'):$this.children(':visible:first'),$previous_image = $current_image .prev() == 'undefined' || $current_image.prev().html() == null ? $this .children(':last'):$current_image.prev(),$next_image = $current_image .next() == 'undefined' || $current_image.next().html() == null ? $this .children(':first'):$current_image.next();if(count>1){$current_image.fadeOut(opts.duration);switch(direction){case -1:$previous_image.fadeIn(opts.duration,function(){clicked = false});break;default:$next_image.fadeIn(opts.duration,function(){clicked = false});break}}}function createNavigation(){var $div = $('<DIV></DIV>'),$next = $div.clone().addClass('sideswap_nav sideswap_next').html(opts.next) .appendTo($parent).hide(),$previous = $div.clone().addClass('sideswap_nav sideswap_previous').html(opts.previous).appendTo($parent).hide();$next.click(function(){if(!clicked){clicked = true;rotate(1)}});$previous.click(function(){if(!clicked){clicked = true;rotate(-1)}});$parent.hover(function(){$next.fadeIn(opts.duration);$previous.fadeIn(opts.duration)},function(){$next.fadeOut(opts.duration);$previous.fadeOut(opts.duration)})}function initiateInterval(){timer = setInterval(function(){rotate()},opts.display_time)}})}})(jQuery);